{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "if": "Bash(git commit:*)",
            "prompt": "If the command is NOT a `git commit`, allow it — this check only applies to commits. If it IS a git commit, extract its -m message and reject unless the message matches `:gitmoji: type(scope): desc` (GFM syntax emojis are ok); when rejecting, say to use /x-commit. Command: $ARGUMENTS",
            "statusMessage": "Validating commit format..."
          }
        ]
      }
    ]
  }
}
